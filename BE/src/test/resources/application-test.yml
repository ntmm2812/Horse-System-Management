spring:
  datasource:
    url: jdbc:h2:mem:horseclub;MODE=MSSQLServer;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: never
app:
  bootstrap:
    enabled: false
  upload-dir: target/test-uploads
logging:
  level:
    root: WARN
    org.springframework: WARN
    com.horsemanagement: INFO
debug: false
